/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    domains: ['edeheyeloakiworbkfpg.supabase.co'],
  },
}

module.exports = nextConfig